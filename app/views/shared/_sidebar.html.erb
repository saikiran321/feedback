<div class="sidebar">
  <br>
  this is the side bar....
  <br>
  Categories:<br>
    <% for tag in Tag.all %>
    <li>
    <%= link_to "#{tag.name.capitalize} (#{Tag.includes(:posts).find(tag.id).posts.length})", tag, class:"sidebar_link" %>
    </li>
  <% end %>
</div>
